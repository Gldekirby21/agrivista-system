"use client";

import React from "react";

/**
 * DECOMMISSIONED PER APPROVED FDD
 * Objective 5 (Historical Crop Yield & Purchase Modeling) is strictly VIEW-ONLY.
 * Forecast generation form is permanently decommissioned.
 */
export const ForecastGenerationForm: React.FC<{
  onForecastGenerated?: (result: any) => void;
}> = () => {
  return null;
};
